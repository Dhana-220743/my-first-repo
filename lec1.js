function add_to_cart(){
    console.log(`product is added to the cart`);
    }
const add_to_wishlist = () => {
    console.log(`product is added to the wishlist`);
    }
function checksubmit(){
    return confirm('Do you want to submit the review?');
}